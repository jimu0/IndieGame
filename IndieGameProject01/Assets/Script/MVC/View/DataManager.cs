using UnityEngine;

namespace Script.MVC.View
{
    public class DataManager : MonoBehaviour
    {

        //public Vector2Int MapSize = new Vector2Int(1, 1);
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
